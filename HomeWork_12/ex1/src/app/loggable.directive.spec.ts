import { LoggableDirective } from './loggable.directive';

describe('LoggableDirective', () => {
  it('should create an instance', () => {
    const directive = new LoggableDirective();
    expect(directive).toBeTruthy();
  });
});
